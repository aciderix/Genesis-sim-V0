import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.genesis.simulator',
  appName: 'Genesis Simulator',
  webDir: 'dist'
};

export default config;
